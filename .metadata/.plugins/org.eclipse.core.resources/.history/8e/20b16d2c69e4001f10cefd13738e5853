@charset "UTF-8";
body {
    font-family: Arial, sans-serif;
    margin: 0;
    padding: 0;
    background-color: #f8f8f8;
}

header {
    display: flex;
    justify-content: space-between;
    align-items: center;
    padding: 15px 20px;
    background-color: #ff914d;
    color: white;
    font-weight: bold;
}

.logo span {
    font-size: 24px;
}

nav ul {
    list-style: none;
    display: flex;
    gap: 20px;
}

nav ul li a {
    text-decoration: none;
    color: white;
}
nav ul li {
	padding:10px;
    transition: background-color 0.3s ease;
}

nav ul li:hover {
    background-color: rgba(256, 146, 80, 0.2);
}

.search-section {
    display: flex;
    justify-content: center;
    margin: 100px 0;
}

.search-section select, .search-section input, .search-section button {
    padding: 10px;
    margin-right: 5px;
    border: 1px solid #ddd;
    border-radius: 5px;
}

.card-container {
    display: flex;
    flex-wrap: wrap;
    justify-content: center;
    gap: 15px;
    padding: 20px 100px;
}

.card {
    background: white;
    padding: 15px;
    border-radius: 10px;
    box-shadow: 0 2px 5px rgba(0, 0, 0, 0.1);
    width: 250px;
    text-align: left;
}

.card>img {
    width: 100%;
    height: 200px;
    border-radius: 10px;
}
